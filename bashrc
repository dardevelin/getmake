#usually distribuitions include a .bashrc file with some configs
#the following line will allow you to have a .bash_aliases in your
#home directory where you have your set of alias

if [ -f ~/.bash_aliases ]; then
    . ~/.bash_aliases
fi

